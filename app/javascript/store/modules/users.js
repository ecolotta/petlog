import axios from '../../plugins/axios.js'

const state = {
 currentUser: null,
};
const mutations = {
  
};
const actions = {
  
};

const getters = {
  currentUser: state => state.currentUser
};

export default {
  namespaced: true,
  state,
  mutations,
  actions,
  getters
}